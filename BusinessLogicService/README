
    Use `python3` for these scripts.

    Manual usage:
    ```
        # pwd = BusinessLogic

        # Venv
            python3 -m venv venv
            source venv/bin/activate
            pip3 install -r requirements.txt

        # Run app
            python3 src/app.py

            # URL: localhost:3004
    ```
